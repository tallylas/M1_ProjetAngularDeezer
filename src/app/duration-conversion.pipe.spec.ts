import { DurationConversionPipe } from './duration-conversion.pipe';

describe('DurationConversionPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationConversionPipe();
    expect(pipe).toBeTruthy();
  });
});
